#bootstrep.yml is loaded at spring bootstrap
spring:
  cloud:
    #Cloud Config for fetching common application config from Central repo like Git
    config:
      enabled: true
      uri: http://localhost:8888
